# Model config (stub).
head: quantile   # or gauss
hidden_dim: 128
num_heads: 4
use_side_info: true
quantiles: [0.01,0.05,0.10,0.25,0.50,0.75,0.90,0.95,0.99]
attention_backend: sdpa  # sdpa | torch_mha | flash | xformers
